# RiskBands 2.1.0 Publication Checklist

Status: READY_FOR_MANUAL_RELEASE. Final local validation completed on 2026-05-16 22:21 -03:00. Do not publish until a human reviewer approves.

## Release Gates

- [x] `pyproject.toml` keeps the optional Spark extra at `pyspark>=3.5,<4`.
- [x] PySpark remains optional for the base install.
- [x] Public release-note text outside `docs-site/` no longer uses final-release blocking status wording.
- [x] `sampling_metadata_` for pandas fit records `sampling_applied=False`, `sampling_strategy="none"`, and matching `n_rows_source`/`n_rows_fit`.
- [x] Spark validation temporary columns use deterministic collision-safe names.
- [x] Spark validation collects only controlled fit samples, one-row fallback samples, or guarded aggregate profile rows.
- [x] `fit_validation_report_` and `transform_validation_report_` remain separate.
- [x] Bundle roundtrip preserves profiles and validation reports; bundle metadata preserves pandas sampling metadata.
- [x] `dist/` was cleaned before final build and contains only 2.1.0 artifacts.
- [x] `docs-site` was updated only after functional gates passed.

## Environment

- Python: `3.11.9` from `.venv-v210-spark`.
- RiskBands import: `riskbands 2.1.0`; `RiskBands is Binner: True`.
- Java: Microsoft OpenJDK `21.0.1+12-LTS`.
- PySpark in validation venv: `3.5.1`.
- PySpark in `[spark]` smoke venv: `3.5.8`.

## Validation Matrix

| Gate | Command | Result |
| --- | --- | --- |
| Ruff | `.\.venv-v210-spark\Scripts\python.exe -m ruff check riskbands tests` | PASS, `All checks passed!` |
| Bandit | `.\.venv-v210-spark\Scripts\python.exe -m bandit -q -r riskbands` | PASS, no findings |
| pip-audit | `.\.venv-v210-spark\Scripts\python.exe -m pip_audit` | PASS after upgrading only venv `pip`/`setuptools`; no known vulnerabilities found; local `riskbands 2.1.0` skipped because it is not on PyPI |
| Full pytest | `.\.venv-v210-spark\Scripts\python.exe -m pytest --basetemp .pytest_tmp/v210-final-polish-all` | PASS, `169 passed, 170 warnings` |
| Spark pytest | `.\.venv-v210-spark\Scripts\python.exe -m pytest -m spark --basetemp .pytest_tmp/v210-final-polish-spark` | PASS, `11 passed, 158 deselected, 162 warnings`, 0 skips |
| Build | `.\.venv-v210-spark\Scripts\python.exe -m build` | PASS outside sandbox after sandbox index failure; built `riskbands-2.1.0.tar.gz` and `riskbands-2.1.0-py3-none-any.whl` |
| Twine | `.\.venv-v210-spark\Scripts\python.exe -m twine check dist/*` | PASS for both artifacts |
| Smoke base | `.pytest_tmp\v210-final-polish-smoke\Scripts\python.exe -m pip install dist/riskbands-2.1.0-py3-none-any.whl` plus import check | PASS outside sandbox after sandbox index failure; `version: 2.1.0`, `RiskBands is Binner: True`, `pyspark_installed: False` |
| Smoke base pip check | `.pytest_tmp\v210-final-polish-smoke\Scripts\python.exe -m pip check` | PASS, no broken requirements |
| Smoke spark | `.pytest_tmp\v210-final-polish-smoke-spark\Scripts\python.exe -m pip install "dist/riskbands-2.1.0-py3-none-any.whl[spark]"` | PASS outside sandbox; installed `pyspark 3.5.8` |
| Smoke spark pip check | `.pytest_tmp\v210-final-polish-smoke-spark\Scripts\python.exe -m pip check` | PASS, no broken requirements |
| Docs-site build | `npm --prefix docs-site run build` | PASS outside sandbox after `spawn EPERM`; Astro check reported 0 errors, 0 warnings, 0 hints; 19 pages built |

## Docs-Site Gate

`DOCS_SITE_UPDATE_ALLOWED=true`

Justification: full pytest, real Spark pytest, build, twine, smoke base, smoke spark, ruff, bandit, and pip-audit passed before final docs-site edits.

## Confirmed Not Done

- [x] No PyPI upload.
- [x] No TestPyPI upload.
- [x] No `v2.1.0` tag; `git tag --list v2.1.0` returned no tag.
- [x] No push; local status still shows uncommitted working-tree changes only.

## Residual Notes

- Spark on this Windows environment prints `ERRO: Acesso negado` after successful Spark exits, but pytest commands returned exit code 0.
- Build, smoke installs, `pip-audit`, and docs-site build needed approved execution outside the sandbox because the sandbox blocked package-index access or process spawn.
